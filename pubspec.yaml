name: static_mapper
description: A Dart package for mapping JSON data to strongly typed models with minimal boilerplate.
version: 1.0.2
repository: https://github.com/altern-digital/static_mapper

environment:
  sdk: ^3.0.0

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
